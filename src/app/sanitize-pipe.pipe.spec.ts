import { SanitizePipePipe } from './sanitize-pipe.pipe';

describe('SanitizePipePipe', () => {
  it('create an instance', () => {
    const pipe = new SanitizePipePipe();
    expect(pipe).toBeTruthy();
  });
});
