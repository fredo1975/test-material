import { TreeTooltipDirectiveDirective } from './tree-tooltip-directive.directive';

describe('TreeTooltipDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TreeTooltipDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
